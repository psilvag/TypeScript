(
    ()=>{
        // argumentos obligatorios
        const fullName=(first:string,last:string)=>{
            return `${first} ${last}`
        }
    const name=fullName('pablo','silva')
    console.log(name);
    }
)