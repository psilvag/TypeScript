"use strict";
(() => {
    let isSuperman = true;
    let isBatman = false;
    console.log(isSuperman);
    console.log(isBatman);
    isBatman = isBatman ? true : false;
    // isBatman y isSuperman solo puede tomar valores boleanos
})();
