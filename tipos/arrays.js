"use strict";
(() => {
    const numbers = [23, 3, 5, 6, 4, 4];
    console.log(numbers);
    const villanos = ['omega rojo', 'duende verde', 'dormau'];
    villanos.forEach(villano => console.log(villano.toUpperCase()));
})();
